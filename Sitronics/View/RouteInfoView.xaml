<UserControl x:Class="Sitronics.View.RouteInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sitronics.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <DataGrid x:Name="routesDataGrid" Width="280" Grid.Column="1" />

        <StackPanel HorizontalAlignment="Left">
            <TextBlock 
               Text="Маршрут"
               Margin="1,10,0,0"
               FontSize="14"
               Foreground="White">
            </TextBlock>
            
            <ComboBox Width="250" x:Name="RoutesComboBox" DisplayMemberPath="Name"/>

            <Button Content="Удалить" Width="140" Margin="5,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
